# 11号线刚柔过渡
- 上行：LC25-29~19/LC004  LC13-02~10/LC110  LC11-01~13/LC001  LC01-01~10
- 下行：RC02-01~10/RC12-05~13/RC14-01~10/RC26-24~30/RC004

1. 按照简单的方法先跑起来
2. 在E盘下放置用户数据
3. claude cli的prompt 给一个文件出来，便于我修改



Web 服务模式（不变）：
python app.py --host 10.10.17.208 --port 8920
模块化测试 — 单独运行任意步骤：
# 仅运行第 6 步（Claude CLI 生成报告）
python app.py --step 6 --task-dir E:\catenary_service\tasks\10.10.17.208\57bb6e933291
# 带参数运行第 6 步
python app.py --step 6 --task-dir E:/catenary_service/tasks/10.10.17.208/abc123 \
    --start-end "K100-K200" \
    --pullout-count "5" \
    --pullout-dec "锚段关节处拉出值偏大"
# 仅运行第 1 步（出图）
python app.py --step 1 --task-dir E:/catenary_service/tasks/10.10.17.208/abc123
工作流程：先通过 Web UI 跑一次完整的 1-5 步（或单独跑 1-5），之后反复调 --step 6 迭代调试，完全跳过前 5 步的重跑。